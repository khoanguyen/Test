Global setup:
 Set up git
  git config --global user.name "Your Name"
  git config --global user.email knguyen@digication.com
        
Next steps:
  mkdir Test
  cd Test
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:khoanguyen/Test.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:khoanguyen/Test.git
  git push -u origin master
      
Importing a Subversion Repo?
  Click here
      
When you're done:
  Continue